import { HighLightBlueDirective } from './high-light-blue.directive';

describe('HighLightBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightBlueDirective();
    expect(directive).toBeTruthy();
  });
});
